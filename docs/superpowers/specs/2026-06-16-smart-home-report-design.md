# 智能家居项目实验报告设计

## 目标
- 生成一个新的 `.docx` 实验报告。
- 版式尽量贴近参考文档 `docx/23125082009刘培冉.docx` 的封面结构、章节层级、图注形式和整体段落风格。
- 报告内容全部替换为当前 `Smart_Home_Analysis` 项目，禁止沿用原参考项目的数据与业务描述。
- 插图仅使用 `assests/` 中现有 6 张截图。

## 用户确认
- 已确认：采用接近参考文档排版样式的方案。
- 已确认：直接生成 `.docx` 成品。
- 已确认：封面信息先沿用参考文档中的学生信息。
- 已确认：采用“高还原成稿”方式生成。

## 报告结构
1. 封面页
2. 学年学期行
3. 实践目的
4. 实践内容
5. 实践过程
6. 部分代码展示
7. 实践体会

## 内容映射
- `实践目的`：改写为智能家居销量数据分析系统的业务背景、课程训练价值、数据分析与可视化目标。
- `实践内容`：写当前项目技术栈、数据来源、系统模块、分析对象。
- `实践过程`：写数据库初始化、后端服务、前端系统、Python 数据分析流程、可视化和预测模块。
- `部分代码展示`：替换为当前项目的后端配置/接口、前端路由与请求逻辑、预测或分析相关代码。
- `实践体会`：结合本项目真实运行与阅读代码体验撰写，不复用参考项目原体会。

## 数据来源
- 项目说明、系统功能说明、API 文档、前后端配置文件、数据库实际数据。
- 目前已验证数据库真实规模：
  - `sales_summary`: 1415 条
  - `region_analysis`: 9 条
  - `product_analysis`: 15 条
  - `promotion_analysis`: 7 条
  - `user_profile`: 4 条
  - `prediction_result`: 1530 条
  - `feature_importance`: 11 条
  - `merged_data`: 9999 条
- 已提取真实时间范围：销售汇总覆盖 `2022-01-02` 至 `2025-11-18`。

## 插图规划
- 共使用 6 张截图，对应以下内容：
  1. 登录界面
  2. 注册界面
  3. 系统首页/数据大屏
  4. 销售趋势或地域分析界面
  5. 产品/促销/用户画像中的一个分析界面
  6. 数据管理页或随机森林预测页
- 图注统一仿参考文档样式，例如 `图3-3-1：登录界面`。

## 版式规则
- 使用与参考文档相同的主要段落样式层级：`Normal`、`Heading 1`、`Heading 2`、`Heading 3`、`Heading 4`。
- 页面边距按参考文档读取结果设置为四边约 2.54cm。
- 不使用表格，保持与参考文档一致。
- 标题编号、段落密度和图片插入节奏尽量贴近参考文档。

## 生成方式
- 使用 `python-docx` 新建一个 `.docx`。
- 读取参考文档结构作为蓝本，但不在原文档上直接替换，避免残留原项目内容。
- 通过脚本插入封面、正文段落、代码块和截图。

## 风险与处理
- 风险：现有截图只有 6 张，无法完全复刻参考文档的图片数量。
  - 处理：保持章节结构一致，减少图片小节数量但保留同类说明。
- 风险：数据库字段存在乱码显示。
  - 处理：正文避免硬写乱码字段名，描述时采用功能性中文表达。
- 风险：用户未提供新的封面身份信息。
  - 处理：当前版本沿用参考文档原封面信息。

## 下一步
- 在用户审阅此设计后，进入 `/write-plan` 阶段，制定 `.docx` 生成与校验步骤。
